; Template for config/eventhandler.conf, copied by the php entrypoint when that
; file is missing. Replaces vagrant/roundx.conf; the only difference is the host,
; which is the compose service name instead of localhost.
[mysql]
host = database
database = etoa
user = etoa
password = etoa

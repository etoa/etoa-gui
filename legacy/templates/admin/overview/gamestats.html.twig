{% extends 'admin/layout/admin.html.twig' %}

{% block content %}
    <h1>Spielstatistiken</h1>

    <h2>Spieler</h2>
    {% if userStats %}
        <p><img src="{{ userStats }}" alt="Userstats" /></p>
    {% else %}
        <p>User Statistik nicht vorhanden!</p>
    {% endif %}

    {% if xmlInfo %}
        <button type="button" onclick="window.open('{{ xmlInfo }}');">Als XML herunterladen</button>&nbsp;
    {% endif %}

    {% if gameStats %}
        {{ gameStats|raw }}
    {% else %}
        <p>Spielstatistiken nicht vorhanden!</p>
    {% endif %}
{% endblock %}

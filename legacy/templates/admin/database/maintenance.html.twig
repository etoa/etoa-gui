{% extends 'admin/layout/admin.html.twig' %}

{% block content %}
    <h1>Datenbank</h1>
    <h2>{{ subTitle}}</h2>

    <p><input type="button" value="Zur Übersicht" onclick="document.location='{{ path('admin.db') }}'"/></p>

    <table class="tb">
        <thead>
        <tr>
            {% for field in fields %}
                <th>{{ field }}</th>
            {% endfor %}
        </tr>
        </thead>
        <tbody>
        {% for row in rows %}
            <tr>
                {% for item in row %}
                    <td>{{ item }}</td>
                {% endfor %}
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <p><input type="button" value="Zur Übersicht" onclick="document.location='{{ path('admin.db') }}'"/></p>
{% endblock %}


Installation
------------

Install requirements:

    apt-get install libboost-thread1.42.0, libboost-regex1.42.0, libboost-filesystem1.42.0, libboost-system1.42.0, libmysql++3

Install etoad from source directory:

    make install

Install etoad from Debian package:

    dpkg -i /path/to/etoa-eventhandler_*.deb



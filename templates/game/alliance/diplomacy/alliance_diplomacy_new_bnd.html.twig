{% extends 'game/alliance/alliance.html.twig' %}
{% import 'macros.html.twig' as macros %}

{% block alliance %}
    {% if msg.success is defined %}
        {{ macros.successMessage(msg.success) }}
    {% endif %}

    {% if msg.error is defined %}
        {{ macros.errorMessage(msg.error) }}
    {% endif %}

    {{ form_start(form) }}
        <twig:Table title="Bündnisanfrage an die Allianz {{ otherAlliance.toString }}">
        {% block table_content %}
            <tr>
                <th>Name des Bündnisses:</th>
                <td>
                    {{form_widget(form.name)}}
                </td>
            </tr>
            <tr>
                <th>Bündnisanfrage:</th>
                <td>
                    {{form_widget(form.text)}}
                </td>
            </tr>
        {% endblock %}
        </twig:Table>
        {{ form_widget(form.send) }}
        <a href="{{ path('game.alliance.diplomacy.relations') }}">
            <input type="button" value="Zurück"/>
        </a>
    {{ form_end(form) }}
{% endblock %}
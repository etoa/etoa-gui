{% extends 'game/alliance/allianceboard/allianceboard.html.twig' %}
{% import 'macros.html.twig' as macros %}

{% block allianceboard %}
    <h2>Kategorie löschen</h2>
    {% if msg.success is defined %}
        {{ macros.successMessage(msg.success) }}
        <a href="{{ path('game.alliance.allianceboard.overview') }}">
            <input type="button" value="Zurück"/>
        </a>
    {% else %}
        {{ form_start(form) }}
        Soll die Kategorie <b>{{ categoryName }}</b> und alle darin enthaltenen Topics und Posts gelöscht werden?<br/><br/>
        {{ form_widget(form.catDelete) }}
        <a href="{{ path('game.alliance.allianceboard.overview') }}">
            <input type="button" value="Abbrechen"/>
        </a>
        {{ form_end(form) }}
    {% endif %}
{% endblock %}
{% extends 'admin/stats/base.html.twig' %}

{% block statsContent %}
    <h2>Kampfwertung</h2>

    <table class="table">
        <thead>
        <tr>
            <th>#</th>
            <th>Nick</th>
            <th>Rasse</th>
            <th>Allianz</th>
            <th>Kämpfe Gewonnen</th>
            <th>Kämpfe Verloren</th>
            <th>Kämpfe Total</th>
            <th>Bewertung</th>
        </tr>
        </thead>
        <tbody>
        {% for rating in ratings %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ rating.user.nick }}</td>
                <td>{{ rating.user.race.name }}</td>
                <td>
                    {% if rating.user.alliance %}
                        {{ rating.user.alliance.tag }}
                    {% endif %}
                </td>
                <td>{{ formatNumber(rating.battlesWon) }}</td>
                <td>{{ formatNumber(rating.battlesLost) }}</td>
                <td>{{ formatNumber(rating.battlesFought) }}</td>
                <td>{{ formatNumber(rating.elorating) }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="99"><p>Keine Spieler gefunden</p></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

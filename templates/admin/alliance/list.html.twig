{% set title="Allianzen" %}

{% extends 'admin/layout/admin.html.twig' %}

{% block breadcrumbs %}
    {{ component('breadcrumbs', { items: [
        { label: 'Allianzsuche', url: path('admin.alliances') },
    ]}) }}
{% endblock %}

{% block content %}

    {{ component('alliance_search', {
        form: form
    }) }}

    <p>Es sind {{ formatNumber(total) }} Einträge in der Datenbank vorhanden.</p>

    <p><a href="{{ path('admin.alliances.new') }}" class="button">Neue Allianz erstellen</a></p>

{% endblock %}

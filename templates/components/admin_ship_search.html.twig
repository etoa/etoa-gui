{% extends 'components/base_search.html.twig' %}

{% block data %}
    <tr>
        <th>Id</th>
        <th>Spieler</th>
        <th>Planet</th>
        <th>Schiff</th>
        <th>Anzahl</th>
        <th>Bunker</th>
        <th>Spezielles</th>
        <th>Aktion</th>
    </tr>
    {% for item in search.entries %}
        {{ component('admin_ship_view', {
            item: item,
            id: 'item-'~item.id
        }) }}
    {% else %}
        <tr><td colspan="9999"><i>Keine Einträge vorhanden</i></td></tr>
    {% endfor %}
{% endblock %}

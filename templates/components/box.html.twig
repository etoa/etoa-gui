<div class="boxLayout {{ class|default('') }}">
    {% if title %}
        <div class="infoboxtitle"><span>{{ title }}</span></div>
    {% endif %}
    <div class="infoboxcontent">
        {% block content %}
        {% endblock %}
    </div>
</div>
{% import 'components/reports/macros.html.twig' as macros %}

Eine Flotte vom Planeten <b>{{ macros.entity(report.entity2) }}</b> hat das Ziel <b>{{ macros.entity(report.entity1) }}</b> um <b>{{ formatTimestamp(report.timestamp) }}</b> erkundet.

{% import 'components/reports/macros.html.twig' as macros %}

Du hast folgendes Angebot (#{{ report.marketReportData.recordId }}) im {{ macros.marketLink(report.entity1) }}
auf {{ macros.entity(report.entity1) }} an {{ macros.user(report.opponent1) }} versteigert:<br/><br/>

{{ macros.marketResourceTable(report) }}

Die Auktion wird nach {{ configValue('market_auction_delay_time')}} Stunden gelöscht und die Waren werden in wenigen Minuten versendet.<br /><br />

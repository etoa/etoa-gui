{% import 'components/reports/macros.html.twig' as macros %}

Du hast folgendes Angebot (#{{ report.marketReportData.recordId }}) von {{ macros.user(report.opponent1) }} gekauft:<br/><br/>
{% if report.content %}
    {{ report.content }}<br/><br/>
{% endif %}

{{ macros.marketResourceTable(report) }}
Die Waren werden vom Marktplatz nach {{ macros.entity(report.entity1) }} geliefert."

{{ macros.fleet2Landing(report) }}

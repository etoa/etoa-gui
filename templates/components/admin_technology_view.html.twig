<tbody
    {{ attributes }}
    data-original-data="{}" {# Hack to prevent issue with symfony/ux #}
    data-action="change->live#update"
>
{% if this.item %}
    {% if this.isEdit %}
        {{ form_start(this.form, {'attr': {'onkeydown' :"return event.key != 'Enter';"}}) }}
    {% endif %}
    <tr>
        <td>{{ this.item.id }}</td>
        <td>{{ this.item.user.nick }}</td>
        <td>{{ this.item.entity.toString }}</td>
        <td>{{ this.item.technology.name }}</td>
        <td>
            {% if this.isEdit %}
                {{ form_row(this.form.currentLevel) }}
            {% else %}
                {{ formatNumber(this.item.currentLevel) }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                {{ form_row(this.form.buildType) }}
            {% else %}
                {{ this.item.buildType }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                {{ form_row(this.form.startTime) }}
            {% else %}
                {{ this.item.buildType ? formatTimestamp(this.item.startTime) : '-' }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                {{ form_row(this.form.endTime) }}
            {% else %}
                {{ this.item.buildType ? formatTimestamp(this.item.endTime) : '-' }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                <a href="#technology-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|submit">Speichern</a>
                <a href="#technology-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|abortEdit">Abbrechen</a>
            {% else %}
                <a href="#technology-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|showEdit">Bearbeiten</a>
                <a onclick="confirm('Soll {{ this.item.technology.name }} {{ this.item.currentLevel}} von diesem Planeten gelöscht werden?')" href="#technology-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|delete">Löschen</a>
            {% endif %}
        </td>
    </tr>
    {% if this.isEdit %}
        {{ form_end(this.form) }}
    {% endif %}
{% endif %}
</tbody>

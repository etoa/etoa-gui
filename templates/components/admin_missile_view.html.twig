<tbody
    {{ attributes }}
    data-original-data="{}" {# Hack to prevent issue with symfony/ux #}
    data-action="change->live#update"
>
{% if this.item %}
    {% if this.isEdit %}
        {{ form_start(this.form, {'attr': {'onkeydown' :"return event.key != 'Enter';"}}) }}
    {% endif %}
    <tr>
        <td>{{ this.item.user.nick }}</td>
        <td>{{ this.item.entity.entity.toString }}</td>
        <td>{{ this.item.missile.name }}</td>
        <td>
            {% if this.isEdit %}
                {{ form_row(this.form.count) }}
            {% else %}
                {{ formatNumber(this.item.count) }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                <a href="#missile-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|submit">Speichern</a>
                <a href="#missile-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|abortEdit">Abbrechen</a>
            {% else %}
                <a href="#missile-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|showEdit">Bearbeiten</a>
                <a onclick="confirm('Sollen {{ this.item.count}} {{ this.item.missile.name }} von diesem Planeten gelöscht werden?')" href="#missile-{{ this.item.id }}" data-action="live#action" data-live-action-param="prevent|delete">Löschen</a>
            {% endif %}
        </td>
    </tr>
    {% if this.isEdit %}
        {{ form_end(this.form) }}
    {% endif %}
{% endif %}
</tbody>

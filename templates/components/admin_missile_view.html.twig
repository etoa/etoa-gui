<tbody
    {{ attributes }}
    data-original-data="{}" {# Hack to prevent issue with symfony/ux #}
>
{% if this.item %}
    <tr>
        <td>{{ this.item.user.nick }}</td>
        <td>{{ this.item.entity.entity.toString }}</td>
        <td>{{ this.item.missile.name }}</td>
        <td>
            {% if this.isEdit %}
                <form data-model="on(change)|*" onkeydown="return event.key != 'Enter';">{{ form_widget(form.count) }}</form>
            {% else %}
                {{ formatNumber(this.item.count) }}
            {% endif %}
        </td>
        <td>
            {% if this.isEdit %}
                <a href="#missile-{{ this.item.id }}" data-action="click->live#action:prevent" data-live-action-param="submit">Speichern</a>
                <a href="#missile-{{ this.item.id }}" data-action="click->live#action:prevent" data-live-action-param="abortEdit">Abbrechen</a>
            {% else %}
                <a href="#missile-{{ this.item.id }}" data-action="click->live#action:prevent" data-live-action-param="showEdit">Bearbeiten</a>
                <a onclick="confirm('Sollen {{ this.item.count}} {{ this.item.missile.name }} von diesem Planeten gelöscht werden?')" href="#missile-{{ this.item.id }}" data-action="click->live#action:prevent" data-live-action-param="delete">Löschen</a>
            {% endif %}
        </td>
    </tr>
{% endif %}
</tbody>
